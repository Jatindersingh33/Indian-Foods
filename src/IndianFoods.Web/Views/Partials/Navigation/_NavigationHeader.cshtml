@inherits UmbracoViewPage<IndianFoods.ContentModels.IHeader>
@{
    var items = Model.HeaderItems;
}
@if (items.Any())
{
    <ul class="Header-info list-inline u-alignCenter">
    @foreach (var item in items)
    { <li>@item</li> }
    </ul>
}